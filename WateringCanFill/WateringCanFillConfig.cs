namespace WateringCanFill;

public class WateringCanFillConfig
{
    // Сколько литров берётся за одно действие
    public float LitresPerTransfer = 1f;

    // Сколько % лейки заполняется за одно действие
    // 20 = 20% от ёмкости
    public float FillPercentPerTransfer = 20f;
}